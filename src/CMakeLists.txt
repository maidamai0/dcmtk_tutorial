file(GLOB srcs *.cpp)
foreach(src ${srcs})
  get_filename_component(example ${src} NAME_WLE)
  add_executable(${example} ${src})
  target_link_libraries(${example} PRIVATE ${DCMTK_ALL_LIBRARIES} spdlog)
  target_compile_definitions(${example} PRIVATE FILE_NAME="${example}")
endforeach(src)
